# Facebook Graph API Service - Environment Variables
# Copy this file to .env and fill in your values

# Server Configuration
PORT=3000
NODE_ENV=development

# Base URL (required for OAuth redirect)
# For local development with ngrok:
# BASE_URL=https://your-ngrok-url.ngrok.io
# For local development (may not work with Facebook OAuth):
# BASE_URL=http://localhost:3000
# For production:
# BASE_URL=https://yourdomain.com
BASE_URL=http://localhost:3000

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/facebook-graph-api-service

# Facebook App Credentials
# Get these from https://developers.facebook.com/apps/
FB_APP_ID=your_facebook_app_id
FB_APP_SECRET=your_facebook_app_secret

# Facebook Webhook Verify Token
# This token is used for webhook verification
# Can be any random string (32+ characters recommended)
# If not set, a default will be used (not recommended for production)
FB_VERIFY_TOKEN=your_verify_token_here

# Request Timeout (optional)
# Timeout for incoming requests in milliseconds
# Default: 6000
# REQUEST_TIMEOUT_MS=6000

# Webhook Forwarding Configuration (optional)
# Timeout for forwarding webhooks to client callbacks (milliseconds)
# Default: 4500
# FORWARD_TIMEOUT_MS=4500

# Retry Configuration (optional)
# Maximum number of retry attempts for failed webhook deliveries
# Default: 5
# MAX_RETRY_ATTEMPTS=5

# Retry Backoff Base (optional)
# Base delay in milliseconds for exponential backoff retries
# Default: 1000
# RETRY_BACKOFF_BASE_MS=1000

# Test Client Webhook Server (optional, for testing)
# Port for the test webhook server (used with npm run test-client)
# Default: 3001
# TEST_CLIENT_PORT=3001

# Test Client Secret Token (optional, for testing)
# Secret token for test webhook server
# Default: test-secret-token-12345
# TEST_CLIENT_SECRET=test-secret-token-12345
